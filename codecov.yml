# Codecov configuration
coverage:
  status:
    project:
      default:
        target: 85%           # Project must maintain 85% coverage
        threshold: 2%         # Allow 2% drop
    patch:
      default:
        target: 90%           # New code should have 90% coverage

comment:
  layout: "reach,diff,flags,files,footer"
  behavior: default
  require_changes: false

ignore:
  - "tests/**/*"
  - "examples/**/*"
  - "docs/**/*"
  - "**/__pycache__"
  - ".venv"
